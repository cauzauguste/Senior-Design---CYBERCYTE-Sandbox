# 1. PostgreSQL / Supabase Configuration

# REQUIRED: The full connection URL for your PostgreSQL database (e.g., Supabase)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
SUPABASE_DB_URL="postgresql://user:secretpassword@db.xyz123.supabase.co:5432/postgres"

# OPTIONAL: Path to the CA certificate file for SSL/TLS database connection.
# Uncomment and set if your database requires SSL connection verification.
# SUPABASE_CA_FILE="/path/to/your/db_ca.crt"

# REQUIRED: The name of the table where telemetry data will be inserted.
SUPABASE_TABLE_NAME="host_telemetry"


# 2. Kafka Configuration

# REQUIRED: Comma-separated list of Kafka broker addresses (host:port).
KAFKA_BROKER="kafka-host-1:9092,kafka-host-2:9092"

# REQUIRED: The Kafka topic for osquery logs.
KAFKA_TOPIC="osquery_logs"

# OPTIONAL: SSL/TLS Configuration (Uncomment all three if using mTLS/SSL)
# These are required if you are using a secure Kafka setup (e.g., Aiven, Confluent Cloud).
# KAFKA_CA_FILE="/path/to/your/kafka_ca.crt"
# KAFKA_CERT_FILE="/path/to/your/client.crt"
# KAFKA_KEY_FILE="/path/to/your/client.key"